#include "libc.h"

int main(){
    // open fd 0 and fd1

    //fork and start sh in child proc


    // waitpid for shell if it exits and if so restart it
    // also wait for orphaned processes 
    for(;;){


    }

}